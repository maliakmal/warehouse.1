<%= simple_form_for(@item) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :picking_order %>
    <%= f.association :product %>
    <%= f.association :location %>
    <%= f.association :location %>
    <%= f.input :qty %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
